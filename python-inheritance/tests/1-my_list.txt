'Test for 1-my_list.py'

'Function being tested: print_sorted()'

>>> MyList = __import__("1-my_list").MyList

Basic MyList usage:

'List of numbers'
    >>> my_list = MyList()
    >>> my_list.append(7)
    >>> my_list.append(-5)
    >>> my_list.append(9)
    >>> my_list.append(0)
    >>> print(my_list)
    [7, -5, 9, 0]

'Empty list'
    >>> my_list = MyList()
    >>> print(my_list)
    
'List of strings'
    >>> my_list = MyList()
    >>> my_list.append("I")
    >>> my_list.append("don't")
    >>> my_list.append("enjoy")
    >>> my_list.append("doing")
    >>> my_list.append("these")
    >>> my_list.append("tests!")
    >>> print(my_list)
    ['I', 'don't', 'enjoy', 'doing', 'these', 'tests!']
    
'List of mixed types'
    >>> my_list = MyList()
    >>> my_list.append(7)
    >>> my_list.append("don't")
    >>> my_list.append(9)
    >>> my_list.append("doing")
    >>> my_list.append("these")
    >>> my_list.append("tests!")
    >>> print(my_list)
    [7, "don't", 9, 'doing', 'these', 'tests!']
